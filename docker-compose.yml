services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: banking
      POSTGRES_PASSWORD: banking
      POSTGRES_DB: banking
    ports:
      - "5433:5432"
    volumes:
      - banking_pg:/var/lib/postgresql/data

volumes:
  banking_pg:
